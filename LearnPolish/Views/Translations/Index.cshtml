@model IEnumerable<LearnPolish.Models.Translation>

@{
    ViewBag.Title = "Index";
}

<h2>Тлумачэнне</h2>

<p>
    @Html.ActionLink("Стварыць", "Create", new { area = "" }, new { @class = "btn btn-success" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Image.Card)
        </th> 
        <th>
            @Html.DisplayNameFor(model => model.Word)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TranslationToPolish)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SwitchedСharacter)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Image.Card)
        </td> 
        <td>
            @Html.DisplayFor(modelItem => item.Word)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TranslationToPolish)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SwitchedСharacter)
        </td>
        <td>
            @Html.ActionLink("Рэдагаваць", "Edit", new { id = item.ID }, new { @class = "btn btn-primary" })
            @Html.ActionLink("Выдаліць", "Delete", new { id = item.ID }, new { @class = "btn btn-danger" })
            @Html.ActionLink("Падрабязнасці", "Details", new { id = item.ID }, new { @class = "btn btn-info" })
        </td>
    </tr>
}

</table>
